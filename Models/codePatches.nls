;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; patches function to save furnitures
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

to correct-bugs
  ask individuals with [time-schedule = 0 or space-schedule = 0 or length time-schedule = 3][die]
  
  ;;CRAZY == AUTODESTRUCTIVE CODE
  let students-set (individuals with [activity = "student"])
  ask n-of (round ((count students-set) / 2)) students-set [die]
  
end